CIRCUITS + DRIVING AUDIO UPDATE
Upload the entire package, including NEW kart-tracks.js at the root next to server.js.
Required: public/index.html, server.js, kart-tracks.js.
Redeploy Render, then refresh every computer.
Title should show Kart Circuits + Audio.
Choose the circuit and existing golf theme in the main menu.

KART RALLY 3D + AIM FIX
If the previous Kart Rally server is already deployed, replace public/index.html and redeploy.
Otherwise upload this whole package, including server.js. Refresh all players.
Title: Kart Rally 3D / Aim Fix.

KART RALLY UPDATE
Upload public/index.html AND server.js together to the existing GitHub repository.
Render: Manual Deploy > Deploy latest commit. Refresh every player browser.
Title should say Kart Rally Update / Controller Fix 12.

GYRO GOLF WORLD TOUR — NEXT STEPS

1. Unzip Gyro-Golf-World-Tour-Render.zip.
2. Open your gyro-golf-world-tour repository on GitHub.
3. Click Add file, then Upload files.
4. Drag in ALL unzipped items: public, server.js, package.json,
   package-lock.json, render.yaml, README.md, scripts, and .gitignore.
5. Commit the files directly to main.
6. In Render click New +, choose Blueprint, connect this repository,
   and click Deploy Blueprint.
7. Open the Render URL. Create a room and give the six-character code
   to Player 2.

PHONE CONTROL NOTE
Click Connect Phone and ALLOW Chrome's local-network permission. The controller
uses timestamp batches when the experiment supports them and automatically
falls back to stable latest-value polling when custom buffers do not. If the
hosted page still blocks the local HTTP phone, open
Settings and click Download Phone-Compatible Game. The Render URL is saved into
that copy automatically.

IMPORTANT MULTIPLAYER NOTE
Upload BOTH public/index.html and server.js from this package. The client now
routes Start Game through the online room and the server locks each player to a
unique browser and phone. Blank local phone rows use mouse controls; they never
share Player 1's phone.

PARTY PACK 2 adds Coin Collect, overhauled Target Golf and Wild Conditions,
43 validated hole architectures, lunar floating rocks, and an online Party
Wheel. Party Wheel is available in multiplayer; its seeded spin and map are
the same on every connected computer.

CONTROL FIX 1 corrects the high-DPI resize loop and gives phone input priority
over optional scenery and trajectory calculations. It also makes all three
party modes visually and physically unmistakable.

CONTROL FIX 2 fixes a Moon Golf theme scope error that could interrupt every
hole during scene setup and leave only the interface over a blue background.

PHYPHOX PRIORITY suspends minimap, scenery traversal, mounts, optional detail,
water sparkle, and shadow refresh while a connected phone is waiting or
swinging. Controller polling remains at the established 25 ms target.

ONLINE FIX 9 adds full-rate batched phyphox samples, smoother live spectators,
online mount/appearance synchronization, and an online lobby that shows only
Stroke Play, Island Hopper, and Party Wheel. Cherry Blossom, Volcanic, and
Aurora Night bring the Course Designer to eight world themes.

CONTROLLER FIX 10 repairs the single-player phyphox regression from Online Fix
9. A controller that cannot serve thresholded buffers now switches immediately
to the original latest-value request instead of appearing live once and then
being disconnected by the stale-phone watchdog.
